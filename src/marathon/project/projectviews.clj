;functions for visualizing and interacting with marathon projects, 
;which are basically just maps with an expected nested structure.
(ns marathon.project.projectviews
  (:require [util [gui :as gui]
                  [table :as tbl]]))

